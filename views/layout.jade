!!! 5
html(lang="en")
  head
    - if(typeof user !== "undefined")
      script
        window.config = {user: !{JSON.stringify(user)}} 
    include header
  body
    .navbar.navbar-static
      .navbar-inner
        .container
          a.brand(href='/') Brasilia USA
          ul.nav.main
            include nav
          ul.nav.user-menu.pull-right
 
    #app.container
      != body

    footer.container
      ul.nav.nav-pills
        include footer
